@model IEnumerable<Academia.Models.db.Nota>

@{
    ViewBag.Title = "Index";
}

<h2><br />Notas</h2>

<p><br />
    @Html.ActionLink("  ", "Create", null, new { @class = "btn btn-primary fa fa-plus", @style = "font-size:x-large" })
</p>
<table class="table">
    <thead>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.actividad)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.porcentaje)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nota1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Asignatura.nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Estudiante.nombre)
        </th>
        <th></th>
    </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.actividad)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.descripcion)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.porcentaje)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.nota1)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Asignatura.nombre)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Estudiante.nombre)
                </td>
                <td>
                    @Html.ActionLink(" ", "Edit", new { id = item.id }, new { @class = "btn btn-light far fa-edit" })
                    @Html.ActionLink(" ", "Details", new { id = item.id }, new { @class = "btn btn-light far fa-eye" })
                    @Html.ActionLink(" ", "Delete", new { id = item.id }, new { @class = "btn btn-light far fa-trash-alt" })
                </td>
            </tr>
        }

    </tbody>

</table>

@section scripts{
    <script>
        $(document).ready(function () {
            $('.table').DataTable();
        });
    </script>
}
